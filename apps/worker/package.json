{
	"name": "worker",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "node --env-file=../web/.env --watch src/index.ts",
		"build": "tsc",
		"start": "node --env-file=../web/.env dist/index.js",
		"check-types": "tsc --noEmit",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@internal-os/db": "workspace:*",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@internal-os/config": "workspace:*",
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
